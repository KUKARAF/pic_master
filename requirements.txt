xxhash
pillow
opencv-python
ultralytics
numpy
fastapi
uvicorn[standard]
jinja2
python-multipart
insightface
# GPU: device is auto-picked (cuda -> xpu[Intel Arc] -> mps -> cpu), overridable
# with MEDIA_DEVICE=cuda|xpu|mps|cpu. For NVIDIA, swap onnxruntime for
# onnxruntime-gpu. For Intel Arc (xpu), swap it for onnxruntime-openvino AND
# install the XPU build of torch (pip install torch --index-url
# https://download.pytorch.org/whl/xpu) on a host with the Intel GPU runtime
# (Level-Zero + compute-runtime). See media_manager/compute.py.
onnxruntime
open_clip_torch
torch
# The `media worker` offload feature (worker_*.py): the worker is a local HTTP
# service (FastAPI/uvicorn, above) and the client talks to it with `requests`;
# both sides serialize msgpack dicts (raw bytes / float32 embeddings round-trip).
msgpack
requests
